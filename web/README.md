# UNWIND web

**SKELETON ONLY.** Task 1 builds no UI. This directory pins Next.js 15 and holds
a single page that says so out loud.

`npm install` has not been run in this repository, so `node_modules/` is absent
and the dependency versions above are **[UNVERIFIED]** — declared, not resolved.

```
cd web && npm install && npm run dev
```
